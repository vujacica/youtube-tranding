# Sirovi i pripremljeni podaci (preveliki za git, dijele se posebno / linkom na Kaggle dataset)
data/
*/_prepared_videos.csv
*/_prepared_trending.csv

# Rezultati izvršavanja upita (log fajlovi, timing JSON-ovi) — generišu se pri svakom pokretanju
**/results/*.txt
**/results/*.json

# Python
__pycache__/
*.pyc
.venv/
venv/

# Docker / OS
.env
.DS_Store
Thumbs.db
